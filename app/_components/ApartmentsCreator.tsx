'use client'
import React, {useState} from 'react';
import { createClient } from '@/utils/supabase/client';

const ApartmentsCreator = () => {
    return (
        <div>
            creating a new apartment here
        </div>
    );
};

export default ApartmentsCreator;